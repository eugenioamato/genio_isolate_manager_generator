name: genio_isolate_manager_generator
description: "The generator for genio_isolate_manager. Forked from isolate_manager_generator with Flutter 3.44.8 compatibility and simplified architecture."
version: 1.0.0
homepage: https://github.com/eugenioamato/genio_isolate_manager_generator
repository: https://github.com/eugenioamato/genio_isolate_manager_generator

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  analyzer: ">=5.12.0 <7.0.0"
  args: ^2.5.0
  path: ^1.9.0
  genio_isolate_manager: ^1.0.0
  yaml: ^3.1.3

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.15

executables:
  genio_isolate_manager_generator: genio_isolate_manager_generator
